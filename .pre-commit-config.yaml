repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: check-added-large-files
        args: ["--maxkb=100000"]  # 100MB 초과 파일 커밋 차단
      - id: end-of-file-fixer      # 파일 끝 개행 정리
      - id: trailing-whitespace    # 불필요한 공백 제거
