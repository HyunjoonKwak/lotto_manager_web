# === 웹 프레임워크 ===
Flask>=2.3,<3.0
Flask-SQLAlchemy>=3.0
Flask-Migrate>=4.0          # Alembic 기반 DB 마이그레이션
Flask-Caching>=2.0
Flask-WTF>=1.1              # 폼 처리 (선택)
Flask-Login>=0.6            # 사용자 인증 (선택)

# === ORM / 데이터베이스 ===
SQLAlchemy>=2.0
alembic>=1.13               # 마이그레이션 백엔드

# === 환경변수 / 설정 ===
python-dotenv>=1.0

# === 유틸리티 ===
requests>=2.31
pandas>=2.2                 # 데이터 처리 (필요하다면)
numpy>=1.26                 # 수치 연산 (필요하다면)

# === 개발 편의 ===
black>=24.0                 # 코드 포맷터
ruff>=0.5                   # 린터
pytest>=8.0                 # 테스트

requests-cache>=1.2
beautifulsoup4
